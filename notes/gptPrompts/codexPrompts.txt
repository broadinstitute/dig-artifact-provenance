

THESE ARE THE CODEX PROMPTS FOR LOADING DATA, CREATING THE DATABASE 


TBD - always update .md file 
- update processs to skip saving to files 
- update process to create edges/nodes with shared nodes, node location, drs_id 
- research DRS and whether server needs to be root path 

- create drs WS 
  - insert method with real location and drs id 
  - verify with 1.4 standard 


20260917 - linter 
update the python script src/python/lint/lint_provenance.py
to take in the dapper.yaml as argument in_schema 
to take in the file to validate as argument in_file_to_validate
make it report a list of errors if there are any
let me know if this is a correct use of the schema file and if there are any concerns or issues


20260815 - schema validation
update the python script src/python/validation/provenance_validator.py
to take in as argument in_schema the schema to validate against
and as argument in_file_to_validate the file to validate against the schema


20260914 - make sure prov is v0.1.0
the raw dapper.yaml schema file for version 0.1.0 is at https://raw.githubusercontent.com/broadinstitute/dapper/0.1.0/schema/dapper.yaml
make sure all the provenance created is as described in the 0.1.0 version 
let me know if this is a correct use of the schema file and if there are any concerns or issues
if you can't acceess the schema file, do nothing

the dapper.yaml schema file for version is at https://github.com/broadinstitute/dapper/blob/0.1.0/schema/dapper.yaml
make sure all the provenance created is as described in the 0.1.0 version at 
let me know if this is a correct use of the schema file and if there are any concerns or issues

update the src/python/onestep/create_bottom_line_dapper_provenance.py python script 
make sure all the provenance created is as described in the 0.1.0 version at 
https://github.com/broadinstitute/dapper/releases/tag/0.1.0 dapper.yaml schema file
let me know if this is a correct use of the schema file and if there are any concerns or issues


20260910 - make creating prov and loading to db one step
when creating the provenance file, use the https://github.com/broadinstitute/dapper/releases/tag/0.1.0 dapper.yaml schema file 
as the annotation_source
let me know if this is a correct use of the schema file and if there are any concerns or issues

update the src/python/onestep/create_bottom_line_dapper_provenance.py python script 
to add in the functionality of the src/python/individualsteps/load_bottom_line_to_db.py python script 
keep the option to save the individual provenance json files as an option 
update the script commnents and the notes/design/onestep/create-bottom-dapper-provenance.md markdown file as needed
let me know if there are any concerns or issues


20260910 - document db loading
ignore the previous task; the new python script has been deleted
for the src/python/load_bottom_line_to_db.py python script 
update the markdown file notes/design/individualsteps/load-bottom-line-to-db.md as documentation on the src/python/load_bottom_line_to_db.py script
in the python script top comments and the md file, insert how to run the script with arguments
document which arguments are mandatory and any default values any aruguments have
let me know if there are any concerns or issues


20260909 - update artifact_id to use verwsion 0.1.0 of dapper - right script this time
update the src/python/onestep/create_bottom_line_dapper_provenance.py python script 
so that it uses as reference the https://github.com/broadinstitute/dapper/releases/tag/0.1.0 version
for all provenance document creation, as far as fields and data
specify what format/data changes are being implemented
let me know if there are any concerns or issues


create the src/python/onestep/create_bottom_line_dapper_provenance.py python script 
combine the src/python/create_bottom_line_graph.py and src/python/create_bottom_line_dapper.py scripts
so that you take in inputs of the create_bottom_line_graph.py script 
and produce the outputs of the create_bottom_line_dapper.py script
simplify as needed
create a markdown file notes/design/create-bottom-dapper-provenance.md as documentation on the new create_bottom_line_dapper_provenance.py script
in the python script top comments and the md file, insert how to run the script with arguments
specify which arguments are mandatory and what are the default values for the arguments
let me know if there are any concerns or issues


ignore the previous task; the changes have been reversed
update the src/python/create_bottom_line_graph.py python script 
reference the https://github.com/broadinstitute/dapper/releases/tag/0.1.0 version
use the computed identifier section to update the relevant identifiers in the provenance generation
update the markdown file notes/design/create-bottom-line-graph.md as needed
in the python script top comments and the md file, insert how to run the script with arguments
specify which arguments are mandatory and what are the default values for the arguments
let me know if there are any concerns or issues




20260908 - update artifact_id to use verwsion 0.1.0 of dapper 
update script src/python/load_bottom_line_to_db.py python script
reference the https://github.com/broadinstitute/dapper/releases/tag/0.1.0 version
use the computed identifier section to update the relevant identifiers in the provenance generation
let me know if there are any concerns or issues



20260907 - add drs table 
update the sqlite schema file at notes/schema/database/provenance_db.sql 
write a sql statement to create a sqlite table prov_edge
with field id which is primary key text,
field name which is a text non null,
field description which is a text nullable,
a field source which is text non nullable and will reference prov_node.id,
a field target which is text non nullable and will reference prov_node.id,
field predicate which is a text non null



20260824 - loading db
modify the src/python/load_bottom_line_to_db.py python script 
add a optional log file input parameter 'in-log-file'
that will default to logs/bottom-line-provenance.log 
If a provenance JSON is malformed, skip the file and log it
log how many files read and how many database record created
log each database record created with the file name read


20260822 - modify db
create a src/python/load_bottom_line_to_db.py python script 
that loads all the files in a directory provided through a 'in-data-dir' argument 
that defaults to data/bottom-line-provenance
into a sqlite database whose file is specified through a 'in-database' argument
insert each provenance file data into the prov_artifact table with the following table fields populated as:
id will be the unique file name prefix
pipeline_type will be 'bottom-line'
provenance will be the json file content
name will be a description of the end result node of the json file contents
the script will first delete all prov_artifact rows of pipeline_type 'bottom-line' before starting the loading process
test the script with the in-database being data/database/provenance_db.sqlite
let me know if there are any concerns or issues


to a sqlite database defined by schema 
and writes out the database to a provided 'out-database-file' argument 
that defaults to data/database/provenance_db.sqlite 


modify the sqlite schema file at notes/schema/database/provenance_db.sql 
add a field pipeline_type as a non null text to the prov_artifact table



20260821 - adding db
write a sqlite schema file at notes/schema/database/provenance_db.sql 
write a sql statement to create a sqlite table prov_artifact 
with field id which is primary key text,
field provenance which is a text non null,
field name which is a text non null,
and field description which is a text nullable


20260821 - generate dapper files
create a markdown file notes/design/create-bottom-line-dapper.md detailing the function and design of the 
src/python/create_bottom_line_dapper.py python script 

create a src/python/create_bottom_line_dapper.py python script 
that reads in the bottom-line pipeline graph data provided through a 'in-graph-file' argument 
and creates a json file for all graph nodes of the s3://dig-open-bottom-line-analysis-stg endpoint directory
create a unique json file name based on the location_path of the file excluding the s3 prefix and using _ to replace /
the files will be written to the data/bottom-line-provenance directory
use the notes/gptRecommendations/bottom-line-dapper.md as a reference for building the dapper provenance json files
test the script using data/graph/provenance_graph.json as an input


20260821 - add in paths to nodes
create a markdown file notes/design/create-bottom-line-graph.md detailing the function and design of the 
src/python/create_bottom_line_graph.py python script 

update the src/python/create_bottom_line_graph.py python script 
using the updated full path listing data in the data/s3 directory as reference
use the web repo https://github.com/broadinstitute/dig-aggregator-methods as the code reference 
add 'location_path' to the nodes which will either be a s3 file or directory for data 
or a github url for the pipeline code


20260820 - fill in gaps to data
update the src/python/create_bottom_line_graph.py python script 
using the updated full path listing data in the data/s3 directory as reference
use the web repo https://github.com/broadinstitute/dig-aggregator-methods as the code reference 
for the bottom-line and intake pipelies
let me know if you are missing an s3 directory listing and what bucket it is

20260819 - investigating listing all s3 dirs and correlate with code to build graph
create a src/python/create_bottom_line_graph.py python script that creates a json file 
that will be saved to data/graph/provenance_graph.json
the json file will contain a 'edges' list element and a 'nodes' list element 
the edges and nodes will denote the graph of dependencies of the bottom-line pipeline 
from the inputs to the endpoint directories of OpenDataTransferStage
each node will either represent a stage of the pipeline or an input or output directory
annotate the edges and nodes as recommended in notes/gptRecommendations/bottom-line-dapper.md 
use the bottom-line code as reference to the pipeline steps taken
use the s3 directory listing files in the data/s3 directory as reference
let me know if you are missing an s3 directory lising and what busket it is



in the bottom-line pipeline, what s3 buckets are used in input and output



20260817 - minimum bottom line input/outputs
is it possible to modify the bottom-line pipleline to emit an input manifest at a step
for each of the open-data published outputs


for each ethnicity phenotype combination of bottom-line outputs, is there a way to determine 
from which variants/ directories the inputs came

can you determine what variamts_raw/ location the intake pipeline read in from 
to produce the variants/ inputs to the  bottom-line pipeline

using notes/gptRecommendations/bottom-line-dapper-input-to-output.md as reference
use input file locations like in s3://dig-analysis-data/variants_raw/GWAS/Agrawal2022_LocalAdiposity_Mixed_females/GFAT/
use output output file locations like in s3://dig-open-bottom-line-analysis-stg/bottom-line/AA/
this might include the intake pipeline as well as bottom-line pipeline
export the above approach to a markdown file
at notes/gptRecommendations/intake-to-bottom-line-dapper.md


using notes/gptRecommendations/bottom-line-dapper.md and 
the provenance file schema at notes/schema/provenance/dapper.yaml as references
map out how to implement provenance documents for the bottom-line pipeline results
focus on all the stages being one block where the input files to the bottotm-line process produce an output
also describe how to map the input files being used by the pipeline block to produce the output results 
into a node/edge provenance graph in the provenance document
export the above approach to a markdown file
at notes/gptRecommendations/bottom-line-dapper-input-to-output.md


look over the provenance file schema at notes/schema/provenance/dapper.yaml
how would I use it to store botton-line pipeline results


- 20260811 - how to use new provenance schema
export the dapper schema application to the bottom-line pipleline above to a markdown file
at notes/gptRecommendations/bottom-line-dapper.md


look over the provenance file schema at notes/schema/provenance/dapper.yaml
how would I use it to store botton-line pipeline results



track the compute commands used to generate the artifacts in a graph format from inouts to outputs


- 20260729 - update to add in gene Methods 

the observable artifact tracking component will also be used for methods in the gene set generation pipeline 
located in repository directory located at /Users/mduby/Code/DccWorkspace/MethodGeneSetExtractors
which is wrapped by an orchestration layer located in repository directory located at /Users/mduby/Code/DccWorkspace/ServerGeneSetCompute
focus on the file artifact generation 
update the recommendations document 


- 20260727 - start project

the observable artifact tracking component can be a thin light layer distributable by pip for the pipeline code to install at runtime
this thin layer can communicate with a REST based server with a flask sqlite backend for persistence
again, minimal adjustment to the pipeline code is desirable
the code should be generated in subdirectories of the src directory
update the recommendations document


analyze the method directory bottom-line in the /Users/mduby/Code/ScalaWorkspace/MethodsBioindex repository
this is a computation framework that runs a method pipeline on aws emr
we need to create an python application in the current /Users/mduby/Code/DccWorkspace/ArtifactProvenance repository directory to track provenance of artifacts created during the 
computation pipeline
the application in the /Users/mduby/Code/DccWorkspace/ArtifactProvenance repository directory should be least disruptive to the existing pipeline code; look to use observable or other annotations
the application should be flexible enough to be used for other pipelines in the /Users/mduby/Code/ScalaWorkspace/MethodsBioindex repository directory
desired technologies are: 
- sqlite for artifact provenance information
- flask for provenance retrieval
- aws s3 for artifact storage
- ga4gh DRS https://github.com/ga4gh/data-repository-service-schemas for artifact versioning and ID tracking 
the python code should be broken down into <function>_utils.py files depending on the method function (sql, web, file, etc)
please summarize your recommendations in a markdown file in the existing directory notes/gptRecommendations


