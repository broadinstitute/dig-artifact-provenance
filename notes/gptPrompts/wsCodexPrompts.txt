

TBD 
- add logging to console for ws/web 
- add port startup log 


20260826 - build web app for testing 
in the web application details page, add a graph display element 
below the pipeline_type element but above the provenance element
the graph display element will use the provenance.edges element to build a graph 
similar to the graph element at url https://translator.broadinstitute.org/genetics_provider/geneset_collections/gene-sets/40
that is built using code at https://github.com/broadinstitute/dig-gene-set-collections-ui
let me know if there are any concerns or issues

update the /list call to return a list of json objects with the row id and name elements in it
update the web application home page to display the name as the link description
update the web application details page to display the name as the main heading of the page
let me know if there are any concerns or issues


build a web page in the flask application 
the home page will have a banner 'Artifact Provenance' 
and will use the /list web serice call to give a listing of the ids returned
with a link to a web page returning the details of the database row 
using the /get_prrovenance call
the row details web page will have a link back to the home page at the top
let me know if there are any concerns or issues



20260825 - add WS calls 
in the /get_provenance REST call
return the prov.artifact.provenance value as the json it is stored in
let me know if there are any concerns or issues


all a /get_provenance REST call to the flask application 
that will take an ID as a parameter 
and return a json object with all the data from the row of the prov_artifact table
each field from the database row should have as key the field name and as value the field value
put all the sql calls in methods in the python-flask-server/db_utils.py file 
log all web service call and any errors
make all method calls handle errors properly
let me know if there are any concerns or issues


build a python flask application 
in the python-flask-server/ directory
it will use the python-flask-server/data/provenance_db.sqlite sqlite database
add a REST call /list that returns a json list of all the id values in the prov_artifact table
in alphabetical order 
the /list call will have a optional limit parameter that will default to 5000 
put all the sql calls in methods in the python-flask-server/db_utils.py file 
make all method calls handle errors properly
log all REST calls into a log file specified by the environment variable WS_PROVENANCE_LOG 
that will default to python-flask-server/logs/ws_provenance.log
log all errors as such
start the service on the port specified by WS_PROVENANCE_PORT environment vatiable that will default to 8080 if not set
let me know if there are any concerns or issues




